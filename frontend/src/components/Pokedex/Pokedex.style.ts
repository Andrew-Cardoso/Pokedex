import { styled } from "@stitches/react";

export const PokedexContainer = styled('article', {
    width: '600px',
    height: 'auto',
    position: 'relative',
    display: 'grid',
    placeItems: 'center',

});